/** @type {import('next-i18next').UserConfig} */
const config = {
  i18n: {
    defaultLocale: 'zh',
    locales: ['zh'],
  },
  defaultNS: 'translations',
  ns: ['translations'],
};

module.exports = config;
